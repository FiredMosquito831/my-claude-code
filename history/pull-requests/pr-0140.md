# #140 — docs: document both Anthropic providers across README, USAGE and the Guide

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-19 01:13:39
- **Merged:** 2026-08-19 01:16:02 as `c22199ba98213cba2c6f4f586d437e4b4835952e`
- **Base → head:** `main` ← `docs/anthropic-providers`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/140
- **Size:** 1 commits, 7 files, +169/-6

## Description

## What

v5.24.0 and v5.25.0 shipped `anthropic` and `anthropic_oauth`, and **no user-facing document mentioned either**. This closes that, and fixes four stale provider counts found on the way.

## Four stale counts, one release

| Where | Was | Now |
| --- | --- | --- |
| README intro | 54 | 56 |
| README Features table | 54 | 56 |
| `docs/USAGE.md` §1 | 54 | 56 |
| `docs/USAGE.md` §7 | 54 | 56 |

`test_readme_provider_table_covers_full_catalog` pins the provider **table** against `PROVIDER_CATALOG` and caught none of these — a prose count is not the table. Same shape as §72, where a target guard didn't protect the sentence beside the target.

## What was added

- **README** — a Features row for Claude-direct, plus an Anthropic subsection under the existing `## OAuth Providers` heading (not a new top-level section).
- **`docs/USAGE.md`** — a "Using Claude models" subsection under Providers, with its own anchor and TOC entry. Existing anchors untouched (§129).
- **In-dashboard Guide** — a `guide-claude-models` section with a TOC entry, because that's where users actually read this.

All three say the same two things: `anthropic` with a Console key is the ordinary supported path; `anthropic_oauth` is **not permitted by Anthropic**, has no "inside Claude Code" exemption, and is gated by default to `cc_entrypoint=cli`.

## The Guide edit nearly shipped broken, twice

1. **I invented `guide-callout` / `guide-callout-warn`** — neither exists in `admin.css`. Per §66, an unstyled class is not an error: the disclaimer would have rendered as flat prose with no visual weight and **nothing would have failed**. Now reuses `.guide-note` with a new `.guide-note-warn` modifier defined beside its base class.
2. **The warn colour is border-and-fill only, never body text.** §144 measured `--warn` as text at **4.1:1** on the light theme — under AA. The rule sets `border-left-color` and `background` and leaves prose on the neutral token, with the reason in a CSS comment so it doesn't get "improved" later.

Checked mechanically before any browser: every class the new section emits has a CSS rule, every `#guide-*` anchor resolves to a real id, and every `var(--token)` in the diff is declared.

## New guards — and these aren't label checks

`anthropic_oauth` uses a credential in a way Anthropic's terms forbid, and the only thing between a user and an account-level consequence is that the docs say so. A future edit trimming the warning "for brevity" would leave the provider shipped and the disclaimer gone.

```
the disclaimer exists and still says "does not permit"
every doc that offers the provider links the disclaimer
the Guide renders it as a warning (guide-note-warn), not as prose
```

**All three confirmed to fail by name** (§134):

```
"does not permit" -> "discourages"   -> disclaimer test FAILED
delete ANTHROPIC-SUBSCRIPTION.md     -> disclaimer test FAILED
guide-note-warn  -> guide-note       -> guide test FAILED
```

**When a feature's safety rests on a sentence, put a test on the sentence.**

## Verification

| Layer | Result |
| --- | --- |
| Full suite | **4214 passed**, 26 skipped |
| Failures | **6**, all `tests/scripts/test_installers.py` PowerShell — documented irreducible baseline, identical to unmodified `main` |
| ruff format / check / ty / suppression grep | clean |

## Version

Bumped **PATCH** (5.25.0 → 5.25.1): the Guide lives under `src/my_claude_code/api/`, a production path per CLAUDE.md, so a bump is required even though the change is documentation content with no behaviour change.

## Residual

- The Guide section was verified structurally (classes, anchors, tokens) but **not opened in a browser** — no server was started for this docs pass. Layout and contrast of the new callout are reasoned-about, not measured.
- Pre-existing and untouched: the README and Guide still don't document the 504-setting Claude Code config editor or image analytics (carried debt).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Y6SPhyApg7trDx44SJqNti

## Commits

- `b0a601050e28` docs: document both Anthropic providers across README, USAGE and the Guide

## Conversation

**@FiredMosquito831** — 2026-08-19 01:15:59

## Browser verification — the residual risk is now closed

I listed "verified structurally but not opened in a browser" as a residual. That's no longer true; a throwaway server on :8191 (fake `HOME`, live :8082 never touched) confirms it.

**Composited contrast of the callout prose, all four themes** — alpha composited down to the first opaque ancestor (§144), transitions disabled before measuring (§141):

| Theme | Prose contrast | Border colour | Border width |
| --- | --- | --- | --- |
| Midnight | **17.97:1** | `rgb(245,158,11)` | 3px |
| Paper (light) | **17.28:1** | `rgb(180,83,9)` | 3px |
| High Contrast | **21.00:1** | `rgb(255,212,0)` | 3px |
| Velvet | **15.61:1** | `rgb(245,176,65)` | 3px |

That is the border-and-fill decision paying off: §144 measured `--warn` **as text** at 4.1:1 on the light theme, under AA. Keeping prose on the neutral token puts every theme between 15.6:1 and 21:1, and the structure is identical across all four (§127 — a callout that changes shape per theme reads as broken).

Also confirmed live:

```
section renders + visible          true
TOC entry present                  true (15 entries)
callout resolves .guide-note-warn  bg rgba(245,158,11,0.08), border-left rgb(245,158,11)
"not permitted" in the section     true
"cc_entrypoint=cli" in the section true
no console errors                  true
```

Cleanup checked with receipts: :8191 re-probed `000` (gone), live :8082 `200`, no `anthropic_oauth.json` or onboarding state written into the real `~/.fcc` (§55c), throwaway home removed.

One thing I noticed and did **not** chase: the action bar shows "1 unsaved change" on a fresh throwaway home with no `.env`. That reproduces independently of this change and is a throwaway-home artifact, not something this PR introduced.

