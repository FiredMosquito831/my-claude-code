# #124 — feat(providers): add qwencloud, qwencloud_coding, agnes, wandb, bedrock, tokenrouter, nararoute

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-15 22:55:10
- **Merged:** 2026-08-15 22:56:44 as `f5f37e26682317ed8f475506f1118b2c70d905a7`
- **Base → head:** `main` ← `feat/trivial-providers`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/124
- **Size:** 1 commits, 18 files, +987/-3

## Description

## What changed

Seven new OpenAI-compatible providers, ported from upstream with our fork's conventions.

| Provider | Type | Notes |
|---|---|---|
| `qwencloud` | QwenCloud Token Plan | OpenAI-compatible, `REASONING_CONTENT` replay |
| `qwencloud_coding` | QwenCloud Coding Plan | same family, coding-plan endpoint |
| `agnes` | Agnes AI gateway | `ChatTemplateReasoning(enable_thinking)` |
| `wandb` | W&B Inference | `ChatTemplateReasoning`, `max_completion_tokens` |
| `bedrock` | Amazon Bedrock Mantle | bearer-token OpenAI-compatible, **no AWS SigV4** |
| `tokenrouter` | TokenRouter gateway | OpenAI-compatible, configurable base URL |
| `nararoute` | NaraRoute gateway | OpenAI-compatible, configurable base URL |

Each follows the standard provider pattern: catalog descriptor + base-URL constant, settings fields (api-key/base-url/proxy), OpenAI-chat profile, provider-manifest override, smoke field, `.env.example`, catalog-order test, runtime-instantiation test, and a focused provider test.

## Verification

- Full suite: **3907 passed / 6 failed** — the 6 are the pre-existing PowerShell-installer checksum failures (baseline unchanged).
- `ruff format --check`, `ruff check`, `ty check`: all clean.

## Notes

- Our fork uses plain `str` settings fields (`default=""`) and a manual smoke-field + `.env.example` list, not upstream's annotated types / auto-generated lists — matched ours exactly.
- The provider-runtime builtin test now seeds the seven new API keys.

## Residual risks

- `bedrock`, `tokenrouter`, `nararoute` ship a default base URL; regional/compute-specific deployments need the corresponding `*_BASE_URL` override (documented in `.env.example`).
- No live chat round-trip has been made against any of the seven (no keys available) — model listing and request-body construction are tested; the first user with a key is the real smoke test.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `5678aad13341` feat(providers): add qwencloud, qwencloud_coding, agnes, wandb, bedrock, tokenrouter, nararoute
