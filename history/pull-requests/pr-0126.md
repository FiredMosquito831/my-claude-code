# #126 — feat(providers): add zenmux, upgrade cline in place, wire tool-turn boundary

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-16 00:05:26
- **Merged:** 2026-08-16 00:07:05 as `607ee4b5279ce4a5632ed88b392a52fecc151732`
- **Base → head:** `main` ← `feat/reasoning-detail-providers`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/126
- **Size:** 1 commits, 14 files, +193/-10

## Description

## What changed

- **`zenmux`** — new ZenMux gateway provider: `ReasoningObject`, `structured_reasoning_details`, `thinking_boolean_path` model list, `reasoning_delta_fallback_field`.
- **`cline` upgraded in place** — kept the `cline` id + `CLINE_API_KEY` env (no rename, no user config breakage), but adopted upstream's richer profile: `DISABLED` reasoning replay, `structured_reasoning_details`, custom `/ai/cline/recommended-models` list.
- **`OpenAIChatProfile`** gains `reasoning_delta_fallback_field` + `structured_reasoning_details` fields and a fallback-aware `reasoning_delta()`.
- **Wired `_close_openai_tool_result_turns`** (deferred from v5.14.2): a `user` role following a `tool` role now inserts a synthetic assistant boundary — fixes a real upstream protocol bug where some OpenAI chat templates reject `user` directly after `tool` output.

## Verification

- Full suite: **3948 passed / 6 failed** (6 = pre-existing PowerShell-installer baseline).
- `ruff format --check`, `ruff check`, `ty check`: all clean.
- Two converter tests updated to the new role ordering + a new boundary test.

## Residual risks

- `cline`'s behavior change (THINK_TAGS → DISABLED replay + structured reasoning) is a provider-profile change for existing `cline` users — flagging it here; if a user's Cline deployment relied on the old reasoning replay, they should re-verify. This is why the id was kept: no config migration needed, just a behavior note.
- `zenmux` has no live round-trip yet (no key available).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `58dbe928f869` feat(providers): add zenmux, upgrade cline in place, wire tool-turn boundary
