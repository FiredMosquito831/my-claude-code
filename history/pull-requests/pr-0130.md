# #130 — feat(rtk): token-optimizer dashboard card + desktop tray toggles

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-16 11:00:44
- **Merged:** 2026-08-16 11:06:16 as `bee5da2a6335546863126955511c22e7e821ed7d`
- **Base → head:** `main` ← `feat/rtk-ui`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/130
- **Size:** 2 commits, 10 files, +570/-2

## Description

## What changed (Part 2 of 2)

Adds the RTK management surfaces, all driving the same `~/.fcc/rtk.json` + `apply_rtk_state` reconciler from v5.20.0.

- **Admin API**: `GET /admin/api/rtk` (status + state) and `POST /admin/api/rtk` (persist + reconcile — RTK is machine-global, so it reconciles unlike the persist-only desktop prefs).
- **Dashboard**: a "Token optimizer" card (three toggles + status line) bound to the new endpoints.
- **Desktop tray**: a "Token optimizer" submenu with three checkable items (Claude Code / Codex / Pi).

CLI, dashboard, and tray all converge on one reconciler, so a toggle anywhere is reflected everywhere.

## Verification

- 12 new tests; full suite **4061 passed / 6 failed** (6 = pre-existing PowerShell-installer baseline).
- `ruff format --check`, `ruff check`, `ty check`: clean.

## Residual risks

- Dashboard toggles and tray menu weren't driven against a live pystray backend or real browser (no DOM harness in the suite); the reconciler call is asserted via monkeypatch. Binary download/verify paths are already covered in `tests/config/test_rtk.py`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `24bc2b23ebdb` feat(rtk): token-optimizer dashboard card + desktop tray toggles
- `6dfce6baee67` fix(rtk): skip tray-menu tests where pystray is unavailable
