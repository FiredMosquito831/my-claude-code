# #108 — feat(admin): dedicated Claude Code page + consistent theme picker

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-13 11:26:34
- **Merged:** 2026-08-13 11:28:16 as `0a836b34e9ab01a17f94e36900a51348fce5ade9`
- **Base → head:** `main` ← `feat/claude-config-page`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/108
- **Size:** 1 commits, 5 files, +89/-58

## Description

## What

Two related UI improvements, built with the `/ui-ux-pro-max:ui-ux-pro-max` skill.

### 1. Claude Code config moves to a dedicated page
The Claude Code settings card lived on the Providers page, competing with 30+ provider cards. It now has its own **"Claude Code"** nav view (between Providers and Model Config), with a page heading explaining the one-click Configure flow.
- New `claude` view in `VIEW_GROUPS` (static view, like Get Started/Guide, so the view-registry guard is safe).
- The `claudeSettingsPanel` section moved from `#view-providers` into a new `#view-claude` admin-view section.
- `loadClaudeSettings()` refreshes when the Claude page is activated (like web_search does).
- Onboarding "Point Claude Code at MCC" step now targets `view="claude"`; Guide step 1 now says "Open the Claude Code page from the left nav."

### 2. Theme picker consistency
The selected pill used `background: var(--accent)` — a full accent fill that recolored per theme (emerald on midnight, crimson on velvet) and read as a broken, uneven control. It now uses the theme's **neutral raised surface** (`--panel-strong`) with an **accent ring + accent text** — identical structure across all four themes (Midnight / Paper / High Contrast / Velvet).

## Verification (real browser, throwaway server :8165, fake home)

- Nav shows "Claude Code" between Providers and Model Config.
- Clicking it: activeView "Claude Code", view visible, Configure/Remove/path controls present.
- Providers page no longer contains `#claudeSettingsPanel`; still has Version + Custom providers.
- Selected theme pill: midnight → `bg rgb(23,27,38)` + `color rgb(16,185,129)`; velvet → `bg rgb(19,29,48)` + `color rgb(230,67,94)` — structurally identical, no full accent fill.
- **The view-registry guard caught a real break:** onboarding step `view` targets are pinned in a hardcoded set in `tests/config/test_onboarding.py`; `claude` had to be added there.
- Full suite: 3706 passed, 26 known environmental failures (identical baseline), 26 skipped. ruff format/check + ty all clean.

## Residual risks

- The Guide "Point Codex or Pi here" section and the troubleshooting section still reference Providers legitimately (they're about providers, not Claude Code config).
- Desktop/terminal screenshots in the Guide still show the old UI (documented in the previous docs PR).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `b356b7f6e6df` feat(admin): move Claude Code config to a dedicated page + consistent theme picker
