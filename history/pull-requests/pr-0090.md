# #90 — fix(install): verify mcc-*/my-claude-code command family in both installers

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-12 00:22:09
- **Merged:** 2026-08-12 00:24:00 as `4db38ad1b23cb6f1466fe6c68fbf643efa885ff7`
- **Base → head:** `main` ← `fix/installer-mcc-verify`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/90
- **Size:** 1 commits, 5 files, +50/-32

## Description

## What changed

The post-install verification in `install.ps1` and `install.sh` only checked the legacy `fcc-*` launchers and asserted `fcc-server --version` == `free-claude-code X`. A fresh install actually creates the full native command family (every `[project.scripts]` entry), but neither installer confirmed the `mcc-*` commands the post-install message tells the user to use.

Both scripts now verify the native family — `mcc-server`, `mcc-claude`, `mcc-claude-old`, `mcc-codex`, `mcc-pi`, `mcc-init`, `mcc-chatgpt-oauth-login`, `mcc-compact-log`, `mcc-help`, `my-claude-code` — resolve inside the uv tool bin and assert `mcc-server --version` == `my-claude-code <version>`. The legacy `fcc-*` aliases resolve through the same distribution, so they exist as soon as the native family does. Dry-run text updated to match.

## Tests

- Harness fixtures now populate both command families and emit the native version string for `mcc-server`; the `fcc-missing` failure path deletes `mcc-server` and asserts the verify loop stops before the version call.
- 26 PS1 installer tests pass; the 6 documented pre-existing PowerShell failures are unchanged.
- `install.ps1` + `install.sh` both parse; `install.sh` behavior verified by Linux CI.

## Residual risks

- None beyond the documented pre-existing PowerShell baseline.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `1a6fcfd1e6d1` fix(install): verify the mcc-* / my-claude-code command family in both installers
