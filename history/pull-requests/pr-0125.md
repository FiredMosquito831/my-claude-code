# #125 — feat(providers): add xai, together, deepinfra, siliconflow, nebius, chutes, featherless

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-15 23:26:41
- **Merged:** 2026-08-15 23:31:29 as `e46c287b01c06c04c52ffaaef0d9a7e07c2f3d1b`
- **Base → head:** `main` ← `feat/b-class-providers`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/125
- **Size:** 1 commits, 18 files, +819/-3

## Description

## What changed

Seven B-class providers ported from upstream. These consume the model-listing machinery landed in v5.14.2.

| Provider | Model list | Notes |
|---|---|---|
| `xai` (Grok) | `/language-models` + `aliases_field` | `REASONING_CONTENT` replay |
| `together` | `/models` filtered `type=chat` | `reasoning_delta_field="reasoning"` |
| `deepinfra` | full-URL `/models/list`, `id_field=model_name` | tags→thinking |
| `siliconflow` | `/models` filtered `sub_type=chat` | |
| `nebius` | `/models` `verbose=true`, `architecture.modality=text->text` | |
| `chutes` | `/models` filtered `input/output_modalities=text` + `tools` | |
| `featherless` | paginated `/models` (`OpenAIModelPagination`) | |

Also ports the `_MINIMAL_TO_XHIGH` effort tuple (was missing; needed by `nebius`).

## Verification

- Full suite: **3942 passed / 6 failed** (6 = pre-existing PowerShell-installer baseline).
- `ruff format --check`, `ruff check`, `ty check`: all clean.

## Residual risks

- No live round-trip against any of the seven (no keys available); model listing and request-body construction are tested, the first user with a key is the real smoke test.
- `featherless` is the first provider to exercise `OpenAIModelPagination` in this fork — its list fetch is covered by the ported machinery tests but not an end-to-end provider round-trip.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `af04b8090d4e` feat(providers): add xai, together, deepinfra, siliconflow, nebius, chutes, featherless
