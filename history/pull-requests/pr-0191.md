# #191 — fix: cross-surface correctness batch — responses usage, atomic migrations, TXT fidelity, extra_body, executor telemetry

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-24 19:14:01
- **Merged:** 2026-08-24 21:08:58 as `b3cc35b9c1aa0ef6355510fa9fc3ce2b58a6d8dd`
- **Base → head:** `main` ← `fix/correctness-smalls`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/191
- **Size:** 1 commits, 20 files, +597/-605

## Description

Five audited fixes, five commits:

1. **Responses usage seeds from message_start** — input_tokens stopped reporting 0 (probe-confirmed).
2. **env migrations write atomically** — tmp+os.replace mirrors the repo convention; unterminated-quote files skip with a warning (probe showed phantom keys inside quoted values).
3. **TXT export stops clipping cells** at 40 chars against its own fidelity contract; >40-char regression added.
4. **extra_body honored on every chat profile** — loud rejecters (kimi/zai/kimi_coding) keep explicit refusal; cohere keeps its allowlist merge.
5. **Executor telemetry**: client cancellations publish the attempt ledger as `interrupted`; post-commit route-ends mark downstream entries; cooldown step-over gains a 5s floor (live rows showed "for 0s"); docstring truthing; RouteExecutionPolicy defaults pinned by test.

§158 note: the briefed "capture-order fix" had no audit item behind it — inspected, found correct as-is, changed nothing.

Verification: 8 mutations caught-by-name; whole touched suites green (192 responses + execution + migrations + export); ruff/ty clean. MINOR slot **5.55.0**.

Residual risks: extra_body is a deliberate wire change on profiles that previously dropped it — canonical-field validation still guards collisions.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `7748c3303833` fix: cross-surface correctness batch — responses usage seed, atomic migrations, TXT fidelity, extra_body everywhere, executor telemetry
