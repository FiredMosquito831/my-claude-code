# #112 — feat(admin): redesign Configure Claude Code page with 'choose how you connect'

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-13 12:39:18
- **Merged:** 2026-08-13 12:40:56 as `bff5af03e541940e9e3950830cf81239055616ef`
- **Base → head:** `main` ← `feat/claude-page-redesign`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/112
- **Size:** 1 commits, 3 files, +175/-13

## Description

## What

Redesigns the Configure Claude Code page (from v5.7.x) to be more readable and give the connection decision a proper home. The page now opens with a dedicated **"Choose how you connect"** section: two flat side-by-side option cards (matching the flat+dense dashboard convention).

### Per session — "Run `mcc-claude`"
- Points only the current shell session at My Claude Code, without touching any file; normal `claude` keeps your standard Anthropic account.
- Two **copyable command blocks**: `mcc-claude` and `mcc-claude --discover-models` — each with a **Copy** button (reusing the existing `guide-copy-button` pattern).
- Note that `--discover-models` also lists the gateway models in the `/model` picker.

### Permanent — "Configure the settings file"
- Writes the proxy URL + token into `settings.json` so every Claude Code session points at My Claude Code.
- Points at the Configure panel below (and the Guide for hand-editing).

## Verification

- Built with `/ui-ux-pro-max` (the skill's command-block + flat+dense guidance; no exact "two-option comparison" rule exists in its DB — said so explicitly).
- Real browser on a throwaway server: both option cards render (badges "Per session" / "Permanent"), both command blocks show Copy buttons, and clicking Copy flips the button to "Copied" (clipboard write succeeded).
- 126 admin/docs/onboarding tests pass; full suite 3706 passed, 26 known environmental failures (identical baseline), zero new regressions. ruff format/check + ty clean.

## Residual risks

- Copy buttons rely on `navigator.clipboard` (already used across the dashboard on the localhost origin); the button still leaves the text selectable if clipboard is unavailable.
- The two-option grid uses `auto-fit minmax(280px, 1fr)`, so on a very narrow viewport the cards stack — intentional, keeps them readable.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `54a00294520d` feat(admin): redesign Configure Claude Code page with "choose how you connect"
