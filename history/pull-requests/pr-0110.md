# #110 — feat(admin): rename page to Configure Claude Code + surface per-session alternative

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-13 12:03:01
- **Merged:** 2026-08-13 12:04:27 as `43bc23a10ebb5a505abb1ad2de04b13e87dd34e6`
- **Base → head:** `main` ← `fix/rename-claude-page`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/110
- **Size:** 1 commits, 4 files, +16/-5

## Description

## What

Quick follow-up to v5.7.0's dedicated Claude Code page:

1. **Rename** the nav label, page title, and heading from "Claude Code" to **"Configure Claude Code"** — it configures the persistent `settings.json`, and the old name read as if it were the only way to connect the CLI.
2. **Surface the per-session alternative** on the page (and in the README): instead of permanently configuring the file, run `mcc-claude` to point only the current shell session at My Claude Code, or `mcc-claude --discover-models` to also list the gateway models in the `/model` picker. Normal `claude` keeps using your standard Anthropic account — so you can have both.

Also updated the Get Started onboarding step instruction to say "Configure Claude Code page".

## Verification

- Real browser on a throwaway server: nav shows "Configure Claude Code", the page renders the Configure button and the `mcc-claude` / `--discover-models` note.
- 126 admin/docs/onboarding tests pass; full suite 3706 passed, 26 known environmental failures (identical baseline), zero new regressions.
- ruff format/check + ty all clean.

## Residual risks

None — label/instruction string change only, no logic touched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `d88fbfd1efa7` feat(admin): rename page to "Configure Claude Code" + surface per-session alternative
