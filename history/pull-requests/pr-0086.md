# #86 — feat: 5.1.0 add mcc-help command and lead installers with mcc commands

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-11 21:07:12
- **Merged:** 2026-08-11 21:08:41 as `c5e123c2aac6fc2ff07972da03f89152a7a7d2a7`
- **Base → head:** `main` ← `feat/mcc-help-and-installer-msg`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/86
- **Size:** 1 commits, 7 files, +101/-14

## Description

## What changed

### New `mcc-help` command

`mcc-help` (legacy alias `fcc-help`) prints a command reference covering every native command, how each is used, the legacy `fcc-*` aliases, and the install-while-running / restart behavior. Registered in `[project.scripts]` and the identity owner map, so `--version` resolves the right owner.

```
My Claude Code -- commands

Start the proxy:
  mcc-server              Start the local proxy and admin dashboard
  my-claude-code          Same as mcc-server (full command name)

Use a coding agent through the proxy:
  mcc-claude              Launch Claude Code through the proxy
  mcc-claude --discover-models   Also enable the model picker from the catalog
  mcc-claude-old          Legacy launcher: full proxy environment, auto-compact
  mcc-codex               Launch Codex through the proxy
  mcc-pi                  Launch Pi through the proxy

Manage and inspect:
  mcc-init                Create or repair ~/.fcc/.env with the config template
  mcc-chatgpt-oauth-login Log in to ChatGPT/Codex via OAuth device flow
  mcc-compact-log         Compact the request log (deduplicate + compress)
  mcc-help                Show this command reference
...
```

### Installers lead with mcc commands

Both installers' post-install message now:
- leads with the `mcc-*` commands and what each does;
- mentions `mcc-help`;
- notes the legacy `fcc-*` commands remain as aliases;
- includes the restart note ("restart the proxy with: mcc-server").

The POSIX/WSL path is unchanged: install-while-running works, and the new version is picked up on the next `mcc-server` restart.

## Verification

- Real WSL install (isolated home) of the LF `install.sh`: installs, verifies `fcc-server --version`, prints the new mcc message.
- `mcc-help` output covers every native command (unit test asserts each is present).
- `ruff`/`ty` clean. Full suite: **16 failed / 3662 passed** — identical failure set to unmodified main (16 pre-existing PowerShell failures), zero regressions.

## Residual risks

- The `mcc-help` text is hand-maintained; the unit test asserts every native command appears but not that descriptions stay accurate if command behavior changes. A future guard could cross-check the launcher docstrings.
- The legacy `fcc-*` commands remain fully functional aliases; they are acknowledged (not advertised) in the help and install messages.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `2cace47dd61d` feat: 5.1.0 add mcc-help command and lead installers with mcc commands
