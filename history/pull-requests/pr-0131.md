# #131 — feat(codex): runtime model-catalog publisher for the Codex App

- **State:** merged
- **Author:** @FiredMosquito831
- **Opened:** 2026-08-16 11:34:34
- **Merged:** 2026-08-16 11:36:58 as `bb136200bdecce6413044b4c53b897ba432e0490`
- **Base → head:** `main` ← `feat/codex-catalog`
- **Original URL:** https://github.com/FiredMosquito831/my-claude-code-legacy/pull/131
- **Size:** 1 commits, 14 files, +625/-26

## Description

## What changed

Makes the **server** own `~/.fcc/codex-model-catalog.json` so an independently-launched Codex App always sees the current model inventory (previously the catalog was only generated on launcher start).

- `runtime/codex_catalog.py` — `CodexModelCatalogPublisher` (refuses an empty projection so it can't wipe a good catalog).
- `runtime/provider_manager.py` — `ModelCatalogPublisher` protocol + publish hooks on `cache_model_infos` / `replace` / refresh / connected-account change; fail-open.
- `providers/runtime/discovery.py` + `model_cache.py` — `warm_referenced_model_cache`, connected-provider ids, add/remove provider.
- `runtime/bootstrap.py` — compose the publisher.
- `cli/launchers/codex_model_catalog.py` — `write_codex_model_catalog` now content-aware + atomic (returns `bool`).
- `README.md` + `ARCHITECTURE.md` — Codex App setup via `~/.codex/config.toml` (using our `env_key=FCC_CODEX_API_KEY` auth style).

## Verification

- 167 targeted tests; full suite **4073 passed / 6 failed** (6 = pre-existing PowerShell-installer baseline).
- `ruff format --check`, `ruff check`, `ty check`: clean.

## Residual risks

- `warm_referenced_model_cache` + `connected_provider_changed` are added and tested directly against the manager but not wired into `ApplicationRuntime.start()` (our runtime has no connected-account concept); the catalog still publishes on each discovery completion, so a fresh install gets it written shortly after startup.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Commits

- `4a884535ac42` feat(codex): runtime model-catalog publisher for the Codex App
